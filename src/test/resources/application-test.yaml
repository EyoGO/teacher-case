db:
  pool:
    size: 12222222
    timeout: 10

spring:
  datasource:
    username: test
    password: test
  jpa:
    properties.hibernate:
        hbm2ddl.auto: validate
logging:
  level:
    org.springframework.test: TRACE
